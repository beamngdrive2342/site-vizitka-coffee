export const CoffeeStreamCorridor = ({ sectionLabel }: { sectionLabel: string }) => {
  // Return null to remove all center guides, dots and labels, keeping only the empty grid space
  return null;
};
